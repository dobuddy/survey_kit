import 'package:flutter/material.dart';

class SurveyDivider extends StatelessWidget {
  const SurveyDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return Divider(
      color: Colors.grey.withOpacity(0.3),
    );
  }
}
